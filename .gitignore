# 确保忽略规则递归生效
Set-Content .gitignore @"
# Build artifacts
/publish/
/**/bin/
/**/obj/
*.zip

# IDE & local
.vscode/
.vs/
.kilocode/
TEMP/
开发记录/
desktop.ini
《WindowsFolderStyleEditor》.txt
history.json
"@

# 把已被跟踪的构建产物移出索引（不删文件）
git rm -r -f --cached --ignore-unmatch -- **/bin **/obj publish
git add .gitignore
git commit -m "chore: ignore build artifacts and untrack bin/ obj/ publish"
# test